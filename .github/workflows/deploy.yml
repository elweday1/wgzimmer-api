name: "Deploooy"
on: push
jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v2

            - name: Deploy
              run: |
                npm install 
                npm install -D wrangler
                npm run deploy
